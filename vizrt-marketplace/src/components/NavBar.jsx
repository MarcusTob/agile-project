import React from 'react';

const NavBar = () => {
    return (
        <nav className="">
            <input type="text" placeholder="Search" className="border-2 rounded border-black"/>
        </nav>
    );
};

export default NavBar;